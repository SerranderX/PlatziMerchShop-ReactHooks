const config = {
    clientIdPaypal: String(process.env.CLIENT_ID_PP),
    apiKeyGoogleMaps: String(process.env.API_KEY_GM),
    apiLocation: String(process.env.API_LOCATION),
}

export default config